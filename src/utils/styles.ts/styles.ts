import { ViewStyle } from "react-native";
import colors from "../colors";

export const stepsContainerStyle: ViewStyle = {
  alignItems: "center",
  backgroundColor: colors.black,
  height: 68,
  justifyContent: "center",
};
